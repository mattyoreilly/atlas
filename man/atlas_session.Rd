% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{atlas_session}
\alias{atlas_session}
\title{A persistent, resumable model-building session}
\description{
An \code{atlas_session} wraps an LLM agent, an R working environment holding your
data, and a run directory on disk. The agent builds models by executing R
code; every code chunk and conversation turn is checkpointed to the run
directory, so a session survives crashes and R restarts (see
\code{\link[=atlas_resume]{atlas_resume()}}). The agent pauses and asks for your approval or input via
the console when it needs a decision (interactive sessions only).
}
\section{Run directory contents}{

\itemize{
\item \code{data.rds}, \code{meta.rds} - inputs, so the session can be rebuilt cold
\item \code{code.R} / \code{code.rds} - every code chunk the agent executed
\item \code{turns.rds} - the full conversation
\item \code{report.md}, \code{leaderboard.csv}, \code{models.rds} - final artifacts
}
}

\examples{
\dontrun{
s <- atlas_session$new(mtcars, outcome = "mpg", n_models = 3)
res <- s$build()          # agent works, asking for approval as needed
res                       # leaderboard + per-model report
s$tell("try a model with only 3 predictors as a 4th candidate")

# later, in a new R session:
s <- atlas_resume(".atlas/20260703-141500")
}
}
\section{Public fields}{
  \if{html}{\out{<div class="r6-fields">}}
  \describe{
    \item{\code{chat}}{The underlying ellmer chat object.}

    \item{\code{env}}{Environment the agent's code runs in (\code{data} lives here).}

    \item{\code{dir}}{Run directory used for checkpoints and artifacts.}

    \item{\code{code}}{Character vector of every code chunk executed so far.}

    \item{\code{test_data}}{Held-out test rows (when \code{test_prop > 0}); never
placed in the agent's environment.}

    \item{\code{tally}}{Live experiment tally: one row per recorded attempt
(\code{attempt}, \code{name}, \code{metric}, \code{value}, \code{best}, \code{verdict}), kept by
atlas from the agent's \code{record_attempt} calls and persisted to
\code{tally.csv} in the run directory.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-atlas_session-initialize}{\code{atlas_session$new()}}
    \item \href{#method-atlas_session-build}{\code{atlas_session$build()}}
    \item \href{#method-atlas_session-check}{\code{atlas_session$check()}}
    \item \href{#method-atlas_session-tell}{\code{atlas_session$tell()}}
    \item \href{#method-atlas_session-results}{\code{atlas_session$results()}}
    \item \href{#method-atlas_session-add_budget}{\code{atlas_session$add_budget()}}
    \item \href{#method-atlas_session-compact}{\code{atlas_session$compact()}}
    \item \href{#method-atlas_session-checkpoint}{\code{atlas_session$checkpoint()}}
    \item \href{#method-atlas_session-print}{\code{atlas_session$print()}}
    \item \href{#method-atlas_session-clone}{\code{atlas_session$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-atlas_session-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-atlas_session-initialize}{}}}
\subsection{\code{atlas_session$new()}}{
  Create a session.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{atlas_session$new(
  data,
  outcome,
  n_models = 3,
  goal = NULL,
  constraints = NULL,
  chat = NULL,
  dir = NULL,
  on_ask = NULL,
  display = c("console", "markdown"),
  stopping_rounds = 3,
  stopping_tolerance = 0.05,
  exclude = NULL,
  interject = NULL,
  autonomous = FALSE,
  test_prop = 0,
  compact_at = 1e+05,
  max_steps = Inf,
  max_runtime = Inf
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{data}}{A data.frame.}
      \item{\code{outcome}}{Name of the outcome column (string).}
      \item{\code{n_models}}{How many final candidate models to build.}
      \item{\code{goal}}{Optional extra instructions for the agent, e.g.
"prioritise interpretability" or a full custom brief.}
      \item{\code{constraints}}{Domain knowledge as hard requirements: a list of
strings and/or \code{\link[=constraint]{constraint()}} objects (see also \code{\link[=con_uses]{con_uses()}},
\code{\link[=con_monotone]{con_monotone()}}). Machine-checked constraints are verified against
every final model.}
      \item{\code{chat}}{An ellmer chat object. Defaults to
\code{ellmer::chat_anthropic()}. Any ellmer provider works.}
      \item{\code{dir}}{Run directory for checkpoints and all output (reports,
validation plots, model bundles). Defaults to a timestamped folder
under \code{getOption("atlas.dir", ".atlas")} - set
\code{options(atlas.dir = "~/atlas-runs")} once to send every run to a
location of your choosing, or pass \code{dir} explicitly per run.}
      \item{\code{on_ask}}{Optional handler for the agent's questions:
\verb{function(question)} returning the user's answer as a string. When
\code{NULL} (default), questions are asked in the console (interactive
sessions) or answered with "use your best judgment" (scripts). Used
by \code{\link[=atlas_app]{atlas_app()}} to route questions to the browser.}
      \item{\code{display}}{How verbose progress is formatted: \code{"console"} (cli
rules and colours) or \code{"markdown"} (fenced code blocks, for
front-ends that render the stream as markdown, like \code{\link[=atlas_app]{atlas_app()}}).}
      \item{\code{stopping_rounds}}{Stopping rule: give up on an iteration (adding
candidates, or a refinement loop) after this many consecutive
attempts without improvement.}
      \item{\code{stopping_tolerance}}{Stopping rule: an attempt only counts as an
improvement if it beats the best validation metric so far by at
least this relative fraction, between 0 and 1 (e.g. \code{0.05} for 5\%).}
      \item{\code{exclude}}{Columns the models must not use (not available at
prediction time, or leakage). They are removed from the data before
the agent ever sees it - the strongest possible guarantee.}
      \item{\code{interject}}{Optional \verb{function()} polled after every tool call.
Return a string to interrupt the agent with a message mid-build (it
is delivered with its next tool result, marked as highest priority);
return \code{NULL} when there is nothing to say. Used by \code{\link[=atlas_app]{atlas_app()}}'s
"Send now" box; front-ends typically read the message from a file or
queue.}
      \item{\code{autonomous}}{Run without a human: the agent states its plan and
proceeds instead of asking for approval, and never calls \code{ask_user}.}
      \item{\code{test_prop}}{Proportion of rows (0 to <1) to hold out as a final
test set \strong{the agent never sees}. Final models are evaluated on it
by Atlas itself after the run (\verb{$results()$test_leaderboard}), so
the comparison can't be gamed by overfitting the agent's own
validation scheme. \code{0} (default) disables the split.}
      \item{\code{compact_at}}{Token budget for the conversation. When the context
grows past this many input tokens, it is compacted before the next
message: the transcript is archived to the run directory, the
window is cleared, and the agent is re-oriented with a briefing
built from the session state (no extra LLM call). Long runs stay
inside the model's context window and stop paying to re-read their
own history. Set to \code{Inf} to disable.}
      \item{\code{max_steps}}{Hard budget: the maximum number of code executions
the agent gets in this session. Unlike the stopping rules (which the
agent applies itself), this is mechanically enforced - past the
limit the \code{run_r_code} tool refuses to execute and instructs the
agent to finalise from what it has. \code{Inf} (default) disables.}
      \item{\code{max_runtime}}{Hard budget: wall-clock seconds for this session
process, enforced the same way as \code{max_steps}. Timing restarts on
\code{\link[=atlas_resume]{atlas_resume()}}. \code{Inf} (default) disables.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-atlas_session-build"></a>}}
\if{latex}{\out{\hypertarget{method-atlas_session-build}{}}}
\subsection{\code{atlas_session$build()}}{
  Run the full model-building loop. Streams the agent's
narration and asks for approval/input in the console when needed.
After the build, machine-checked constraints are verified against
every final model; violations are sent back to the agent to fix, up
to \code{max_fix_rounds} times.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{atlas_session$build(
  verbose = TRUE,
  max_fix_rounds = 2,
  refine = TRUE,
  validate = TRUE
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{verbose}}{Show the agent's narration, code, and output live.}
      \item{\code{max_fix_rounds}}{How many constraint-repair rounds to allow.}
      \item{\code{refine}}{After the winning algorithm is found (and constraints
pass), iterate on its feature selection and engineering - one change
per attempt, same validation scheme - until the session's stopping
rules trigger (\code{stopping_rounds} attempts without a \code{stopping_tolerance} gain).
The refined model is added to the results alongside the original.}
      \item{\code{validate}}{Produce reviewable validation output (gain,
calibration, grouped residuals, one-ways, PDPs) for the winning
model as interactive HTML files in the run directory. Needs the
\code{modelblueprint} package; silently skipped when it isn't installed.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    An \code{atlas} results object (invisibly); see \verb{$results()}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-atlas_session-check"></a>}}
\if{latex}{\out{\hypertarget{method-atlas_session-check}{}}}
\subsection{\code{atlas_session$check()}}{
  Verify all machine-checked constraints against the
current \code{atlas_models}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{atlas_session$check()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A data.frame with one row per model x constraint: \code{model},
\code{constraint}, \code{passed} (\code{NA} for prompt-only constraints), \code{detail}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-atlas_session-tell"></a>}}
\if{latex}{\out{\hypertarget{method-atlas_session-tell}{}}}
\subsection{\code{atlas_session$tell()}}{
  Send a follow-up instruction or question to the agent in
the same context ("why did you drop cyl?", "add a 4th model", ...).
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{atlas_session$tell(text, verbose = TRUE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{text}}{What to say to the agent.}
      \item{\code{verbose}}{As in \verb{$build()}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The agent's reply (invisibly).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-atlas_session-results"></a>}}
\if{latex}{\out{\hypertarget{method-atlas_session-results}{}}}
\subsection{\code{atlas_session$results()}}{
  Collect results: the fitted models, the leaderboard, the
report, and the full code trail.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{atlas_session$results()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    An object of class \code{atlas}: list with \code{models} (named list of
fitted models), \code{leaderboard} (data.frame of validation metrics),
\code{report} (markdown), \code{code}, \code{dir}, and \code{session} (this object).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-atlas_session-add_budget"></a>}}
\if{latex}{\out{\hypertarget{method-atlas_session-add_budget}{}}}
\subsection{\code{atlas_session$add_budget()}}{
  Grant the agent more mechanical budget. The hard caps
(\code{max_steps}, \code{max_runtime}) protect unattended runs, but they also
bind follow-up \verb{$tell()} calls on a finished session - top the
budget up explicitly when you want more work done:
\code{res$session$add_budget(steps = 25)}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{atlas_session$add_budget(steps = 0, seconds = 0)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{steps}}{Additional code executions to allow.}
      \item{\code{seconds}}{Additional wall-clock seconds to allow.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-atlas_session-compact"></a>}}
\if{latex}{\out{\hypertarget{method-atlas_session-compact}{}}}
\subsection{\code{atlas_session$compact()}}{
  Compact the conversation to save tokens: archive the
transcript to the run directory, clear the context window, and
re-orient the agent with a state briefing on the next message. The
R environment (models, data) and code log are untouched - they are
the durable memory. Called automatically when the context exceeds
\code{compact_at}; call it yourself before a long follow-up to start
from a lean window.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{atlas_session$compact()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-atlas_session-checkpoint"></a>}}
\if{latex}{\out{\hypertarget{method-atlas_session-checkpoint}{}}}
\subsection{\code{atlas_session$checkpoint()}}{
  Write the current code log and conversation to the run
directory. Called automatically after every tool call and reply.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{atlas_session$checkpoint()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-atlas_session-print"></a>}}
\if{latex}{\out{\hypertarget{method-atlas_session-print}{}}}
\subsection{\code{atlas_session$print()}}{
  Print a short status line.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{atlas_session$print(...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Ignored.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-atlas_session-clone"></a>}}
\if{latex}{\out{\hypertarget{method-atlas_session-clone}{}}}
\subsection{\code{atlas_session$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{atlas_session$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
