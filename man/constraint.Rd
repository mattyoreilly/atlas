% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints.R
\name{constraint}
\alias{constraint}
\title{Define a hard constraint on the models Atlas builds}
\usage{
constraint(description, check = NULL)
}
\arguments{
\item{description}{What must hold, in plain language. The agent reads this.}

\item{check}{Optional verification: \verb{function(model, data)} returning
\code{TRUE} if the constraint holds, or a string describing the violation
(\code{FALSE} also counts as a violation). Errors count as violations too, so
a check may simply \code{stop()} on bad models.}
}
\value{
An \code{atlas_constraint}.
}
\description{
Constraints carry domain knowledge into the build: the description is put
in front of the agent as a hard requirement, and if \code{check} is supplied the
constraint is \emph{verified} - every final model is tested after the build, and
violations are sent back to the agent to fix (see \code{max_fix_rounds} in
\code{\link[=atlas]{atlas()}}). Plain strings passed to \code{constraints} are shorthand for
\verb{constraint(<string>)}: enforced via instructions only.
}
\examples{
constraint("never use the `id` column; it is a row identifier")

constraint(
  "predictions must never be negative",
  check = function(model, data) {
    p <- predict(model, newdata = data)
    if (all(p >= 0)) TRUE else "some predictions are negative"
  }
)
}
\seealso{
\code{\link[=con_uses]{con_uses()}}, \code{\link[=con_monotone]{con_monotone()}} for ready-made checks.
}
