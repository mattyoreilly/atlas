% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints.R
\name{extract_constraints}
\alias{extract_constraints}
\title{Extract constraints from a natural-language brief}
\usage{
extract_constraints(text, data = NULL, chat = NULL)
}
\arguments{
\item{text}{The brief, in plain language. Sentences, bullets, whatever.}

\item{data}{Optional data.frame the constraints are about; its column
names are given to the parser, and extracted variable names are
validated against them (a constraint naming an unknown column falls
back to prompt-only enforcement).}

\item{chat}{An ellmer chat object; defaults to \code{ellmer::chat_anthropic()}.}
}
\value{
A named list of \code{\link[=constraint]{constraint()}} objects, ready for the
\code{constraints} argument of \code{\link[=atlas]{atlas()}} or \link{atlas_session}.
}
\description{
Uses the LLM to parse free text into Atlas constraints, mapping to
machine-checked types wherever possible: "wt must be used" becomes
\code{\link[=con_uses]{con_uses()}}, "mpg should never increase with weight" becomes
\code{\link[=con_monotone]{con_monotone()}}, and anything else becomes a prompt-enforced
\code{\link[=constraint]{constraint()}}. Returns the list for you to inspect (print it!) before
passing to \code{\link[=atlas]{atlas()}} - the parse is a judgment call, so review it.
}
\examples{
\dontrun{
cons <- extract_constraints(
  "weight must be in the model; predictions can never go up as
   horsepower rises; qsec is post-hoc, never use it",
  data = mtcars
)
cons  # inspect: which became machine checks?
res <- atlas(mtcars, "mpg", constraints = cons)
}
}
