% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints.R
\name{con_monotone}
\alias{con_monotone}
\title{Constraint: predictions must be monotonic in a variable}
\usage{
con_monotone(
  var,
  direction = c("increasing", "decreasing"),
  tol = 1e-08,
  n_grid = 25,
  n_rows = 20
)
}
\arguments{
\item{var}{Column name (numeric) the response must be monotonic in.}

\item{direction}{\code{"increasing"} or \code{"decreasing"}.}

\item{tol}{Tolerance for tiny numeric wiggles.}

\item{n_grid, n_rows}{Size of the sweep grid and number of rows tested.}
}
\value{
An \code{atlas_constraint}.
}
\description{
Machine-checked, model-agnostic (ICE-style): for a sample of observed rows,
the variable is swept over its observed range with everything else held
fixed, and predictions must move in the stated direction every time.
}
\details{
Monotonicity is non-strict, so a model that does not use \code{var} at all
passes trivially (a flat response is monotone). This makes the constraint
conditional — "if the model responds to \code{var}, the effect must be
monotone" — without forcing the variable in. Pair with \code{\link[=con_uses]{con_uses()}} when
the variable must also be used.
}
\examples{
\dontrun{
atlas(df, "price", constraints = list(
  mono_sqft = con_monotone("sqft", "increasing")
))
}
}
