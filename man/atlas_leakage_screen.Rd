% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints.R
\name{atlas_leakage_screen}
\alias{atlas_leakage_screen}
\title{Screen predictors for possible target leakage}
\usage{
atlas_leakage_screen(data, outcome, threshold = 0.95, max_rows = 5000)
}
\arguments{
\item{data}{A data.frame.}

\item{outcome}{Name of the outcome column.}

\item{threshold}{Flag predictors with univariate R-squared at or above
this value.}

\item{max_rows}{Screen at most this many rows (evenly spaced) for speed.}
}
\value{
A data.frame with \code{variable}, \code{r2}, and \code{flagged}. High-cardinality
factors (e.g. IDs) get \code{r2 = NA}: they can't be assessed this way, but
are usually unusable as predictors anyway.
}
\description{
A quick, model-free screen: for each predictor, how much of the outcome
does it explain on its own (R-squared of \code{lm(outcome ~ predictor)}, with
factor outcomes coded to integers)? Near-perfect univariate fits usually
mean the column is derived from the outcome or recorded after it - classic
leakage. Used automatically by \code{\link[=atlas]{atlas()}} to warn the agent, and by
\code{\link[=atlas_app]{atlas_app()}} to flag columns in the feature picker; also useful on its
own before any modeling.
}
\examples{
leaky <- mtcars
leaky$mpg_copy <- leaky$mpg * 2
atlas_leakage_screen(leaky, "mpg")
}
